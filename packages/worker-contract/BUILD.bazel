filegroup(
    name = "schemas",
    srcs = glob(
        ["**/*.md", "**/*.proto", "**/*.yaml", "**/*.yml"],
        allow_empty = True,
    ),
)

filegroup(
    name = "plugin_testdata",
    srcs = glob(["testdata/*.json"]),
    visibility = ["//visibility:public"],
)
